"""One-shot seed scripts for dimensional reference data.

ADR-010 keeps schema migrations in `migrations/` (goose). Dimension data
that is too large or too name-heavy to inline in a migration lives here.
"""
